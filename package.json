{
  "name": "webglmath",
  "devDependencies": {
    "babel-jest": "^19.0.0",
    "babel-preset-env": "^1.4.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "eslint": "latest",
    "glob": "latest",
    "jest": "^19.0.2",
    "react-test-renderer": "^15.5.4",
    "webpack": "latest"
  },
  "scripts": {
    "build": "webpack -p",
    "analyze": "coala --ci --files=js/*.js --bears=ESLintBear",
    "test": "jest --coverage"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "<rootDir>/(old_test)/"
    ]
  }
}
